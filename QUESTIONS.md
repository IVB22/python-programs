# Python Programming Questions

This repository contains Python programming exercises covering various topics.

## Questions List

### 1. Basic Programming
**a)** Write a program that asks the user for a weight in kilograms and converts it to pounds (2.2 pounds = 1 kg)

**b)** Write a program that uses a for loop to print: 8, 11, 14, 17, 20, ..., 83, 86, 89

### 2. Split a string into array of characters in python

### 3. Write a python program to get the largest number from a list

### 4. Write a python program to calculate the nth Fibonacci number using recursion

### 5. Write a python program that defines a car class with attributes (make, model, year) and methods (start(), stop())

### 6. Write a Python program demonstrating inheritance with base class Animal and derived classes (Dog, Cat, etc.)

### 7. Define a base class 'animal' with make_sound(). Implement derived classes (Dog, cat, Bird) that override make_sound() to demonstrate polymorphism

### 8. Write a python program demonstrating error handling using try-except for division by zero

### 9. NumPy Programs
**a)** Test whether none of the elements of a given array is zero using NumPy methods

**b)** Create element-wise comparison of two arrays (greater, greater_equal, less, less_equal, equal, allclose)

### 10. Extract all numbers from a given array which are less and greater than a specified number using NumPy

### 11. Find the indices of maximum and minimum numbers along given axis using NumPy

### 12. Pandas Data Series: Create and display a one-dimensional array-like object

### 13. Pandas Data Series: Convert Pandas Series to Python list and check types

### 14-17. Pandas DataFrames
Use this sample data:
```python
Exam_data = {
    'name': ['Anastasia', 'Dima', 'Katherine', 'James', 'Emily', 'Michael', 'Matthew', 'Laura', 'Kevin', 'Jonas'],
    'score': [12.5, 9, 16.5, np.nan, 9, 20, 14.5, np.nan, 8, 19],
    'attempts': [1, 3, 2, 3, 2, 3, 1, 1, 2, 1],
    'qualify': ['yes', 'no', 'yes', 'no', 'no', 'yes', 'yes', 'no', 'no', 'yes']
}
Labels = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'I', 'j']
```

**14)** Create and display DataFrame with index labels

**15)** Change name 'James' to 'Suresh' in DataFrame

**16)** Insert a new column in existing DataFrame

**17)** Get list from DataFrame column headers

### 18. Create a series of plots to analyze a given dataset

### 19. Generate subplot layout with various plot types (scatter, line, bar)

### 20. Visualize time-series data and customize axis labels and date formats

### 21. Create a 3D scatter plot

---

## Files

Each question has a corresponding Python file named appropriately (e.g., `1a_kg_to_pounds.py`, `2_split_string.py`, etc.)
